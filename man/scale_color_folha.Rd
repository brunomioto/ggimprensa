% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tema_folha.R
\name{scale_color_folha}
\alias{scale_color_folha}
\title{Escala de cor para a paleta de cores Folha}
\usage{
scale_color_folha(..., na.value = "grey50")
}
\arguments{
\item{...}{Argumentos adicionais passados para \code{discrete_scale}.}

\item{na.value}{A cor a ser usada para valores ausentes. O valor padrão é \code{"grey50"}.}
}
\value{
Uma função de escala do ggplot2 que aplica a paleta de cores Folha.
}
\description{
Uma escala personalizada do ggplot2 para a estética de `color` usando a paleta de cores Folha. A paleta é composta por um conjunto de nove cores distintas, projetadas para serem usadas em variáveis discretas de cor em gráficos do ggplot2.
}
\examples{
# Exemplo de uso com ggplot
library(ggplot2)
ggplot(mtcars, aes(factor(cyl), color = factor(cyl))) +
  geom_bar() +
  scale_color_folha()

# Exemplo com o parâmetro `na.value` personalizado
ggplot(mtcars, aes(factor(cyl), color = factor(cyl))) +
  geom_bar() +
  scale_color_folha(na.value = "grey80")
}
